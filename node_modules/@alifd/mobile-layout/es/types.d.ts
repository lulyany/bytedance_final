/**
 *pro layout types
 */
import { CSSProperties, HTMLAttributes, RaxNode } from 'rax';
export declare type BaseSize = 'small' | 'medium' | 'large';
export interface TypeMark {
    _typeMark: string;
}
/**
 * 基础属性
 */
export declare type BaseProps = Omit<HTMLAttributes<HTMLElement>, 'title'>;
/**
 * 基础的小布局简写配置
 */
export declare type BaseGap = 'auto' | number;
interface BaseBgMode {
    /**
     * 容器预置背景色
     */
    mode?: 'surface' | 'lining' | 'transparent';
}
/**
 * 断点
 */
export interface BreakPoint {
    /**
     *  断点宽度(包含)
     */
    width: number;
    /**
     * 最大展示宽, 默认为断点宽度
     */
    maxContentWidth: number;
    /**
     * 列数
     */
    numberOfColumns: 1 | 2 | 4 | 8 | 12 | 16;
}
/**
 * 断点配置(至少 2 个)
 */
export declare type BreakPoints = [BreakPoint, BreakPoint, ...BreakPoint[]];
/**
 * 上下文
 */
export interface LayoutContextProps {
    /**
     * 类名前缀
     */
    prefix?: string;
    /**
     * 当前断点信息
     */
    breakPoint?: BreakPoint;
    /**
     * 禁用页面内容的内边距
     */
    noPadding?: boolean;
    /**
     * 最大列数
     */
    maxNumberOfColumns: number;
    /**
     * content 内部章之间的间距
     */
    sectionGap?: BaseGap;
    /**
     * 水槽间距
     */
    blockGap?: BaseGap;
    /**
     * 小布局间距（行、列、网格布局的 单元格-Cell 间距）
     */
    gridGap?: BaseGap;
}
/**
 * 序章
 */
export interface PageHeaderProps extends BaseProps, BaseBgMode {
    /**
     * 显示分割线
     */
    divider?: boolean;
    /**
     * 固定在页面顶部
     */
    fixed?: boolean;
    /**
     * 禁用底部内边距
     */
    noBottomPadding?: boolean;
    /**
     * 内部容器样式名
     */
    containerClassName?: string;
    /**
     * 内部容器自定义样式
     */
    containerStyle?: CSSProperties;
    /**
     * 子节点
     */
    children?: RaxNode;
}
/**
 * 终章
 */
export interface PageFooterProps extends BaseProps, BaseBgMode {
    /**
     * 显示分割线
     */
    divider?: boolean;
    /**
     * 固定在页面底部
     */
    fixed?: boolean;
    /**
     * 禁用顶部内边距
     */
    noTopPadding?: boolean;
    /**
     * 子节点
     */
    children?: RaxNode;
    /**
     * 内部容器样式名
     */
    containerClassName?: string;
    /**
     * 内部容器自定义样式
     */
    containerStyle?: CSSProperties;
}
/**
 * 内容
 */
export interface PageContentProps extends BaseProps, BaseBgMode {
    /**
     * 子节点
     */
    children?: RaxNode;
    /**
     * 禁用内容区域的默认内间距
     */
    noPadding?: boolean;
}
/**
 * 页面
 */
export interface PageProps extends PageContentProps {
    /**
     * class 前缀
     */
    prefix?: string;
    /**
     * 自定义 breakpoints
     */
    breakPoints?: BreakPoints;
    /**
     * 监听窗口尺寸变化
     */
    responsive?: boolean;
    /**
     * 章之间的间距（若未指定单位，默认为 rpx)
     */
    sectionGap?: BaseGap;
    /**
     * 水槽间距（若未指定单位，默认为 rpx)
     */
    blockGap?: BaseGap;
    /**
     * 小布局间距（行、列、网格布局的 单元格-Cell 间距）
     */
    gridGap?: BaseGap;
    /**
     * 子节点 两种模式：
     *  1. 有 Header, Content, Footer节点之一时，过滤其他 children 的其他节点;
     *  2. Header, Content, Footer 都没有时，自动包裹 Content 节点，形如：Page > Content > children
     */
    children?: RaxNode;
    /**
     * 断点更改时触发 `responsive` 启用时生效
     * @param curBreakPoint
     * @param prevBreakPoint
     */
    onBreakPointChange?: (curBreakPoint: BreakPoint, prevBreakPoint: BreakPoint, breakPoints?: BreakPoints) => void;
}
/**
 * 章节
 */
export interface SectionProps extends BaseProps {
    /**
     * 章节标题
     */
    title?: RaxNode;
    /**
     * 附加内容
     */
    extra?: RaxNode;
    /**
     * 章节中区块的间隙
     */
    gap?: BaseGap;
    /**
     * 子节点
     */
    children?: RaxNode;
}
/**
 * 单元格容器
 */
export interface CellProps extends BaseProps {
    /**
     * 在行模式下，未设置 autoFit 时, 自定义单个 cell 的宽度，
     */
    width?: number | string;
    /**
     * 在列模式下，未设置 autoFit 时, 自定义单个 cell 的高度，
     */
    height?: number | string;
    /**
     * 是否宽度（行模式下）/ 高度（列模式下）自适应
     */
    autoFit?: boolean;
    /**
     * 内容间隙
     */
    gap?: BaseGap;
    /**
     * 主轴方向
     */
    direction?: 'hoz' | 'ver';
    /**
     * cell 子元素的水平对齐方式
     */
    align?: 'left' | 'center' | 'right';
    /**
     * cell 子元素的垂直对齐方式
     */
    verAlign?: 'top' | 'middle' | 'bottom' | 'space-around' | 'space-between' | 'space-evenly';
    /**
     * 启用 display:block 模式(默认为 flex )
     */
    block?: boolean;
    /**
     * 孩子节点
     */
    children?: RaxNode;
}
/**
 * 区块，默认 flex 布局
 */
export interface BlockProps extends BaseProps, BaseBgMode, Omit<CellProps, 'width' | 'height'> {
    /**
     * 禁用内间距
     */
    noPadding?: boolean;
    /**
     * 展示分割线
     */
    divider?: boolean;
    /**
     * 列宽
     */
    span?: number;
    /**
     * 显示有边框
     */
    bordered?: boolean;
    /**
     * 区块标题
     */
    title?: RaxNode;
    /**
     * 标题的对齐方式
     */
    titleAlign?: CellProps['align'];
    /**
     * 有标题时生效,内容区域的样式名
     */
    contentClassName?: string;
    /**
     * 有标题时生效,内容区域的自定义样式
     */
    contentStyle?: CSSProperties;
    /**
     * 附加内容
     */
    extra?: RaxNode;
    /**
     * 孩子节点
     */
    children?: RaxNode;
}
/**
 *  cell 父容器组件
 */
export interface GridProps extends BaseProps {
    /**
     * 默认行列间距, `BaseGap`  可为预设值或 css 单位
     */
    gap?: [BaseGap, BaseGap];
    /**
     * 行数
     */
    rows?: number;
    /**
     * 列数
     */
    cols?: number;
    /**
     * 单元格的最小宽度
     */
    minWidth?: number;
    /**
     * 单元格的最大宽度
     */
    maxWidth?: number;
    /**
     * 行间隙,可为预设值 `small`， `medium`、`large`, `false` 或 css 长度单位。此项设置会覆盖 Page 上的 grid-gap 全局值
     */
    rowGap?: BaseGap;
    /**
     * 列间隙,可为预设值 `small`， `medium`、`large`, `false` 或 css 长度单位。此项设置会覆盖 Page 上的 grid-gap 全局值
     */
    colGap?: BaseGap;
    /**
     * 水平对齐方式
     */
    align?: 'left' | 'center' | 'right' | 'stretch';
    /**
     * 垂直对齐方式
     */
    verAlign?: 'top' | 'middle' | 'bottom' | 'stretch' | 'baseline';
    /**
     * 自定义渲染网格项
     */
    renderItem?: (row: number, col: number) => RaxNode;
    /**
     * 孩子节点
     */
    children?: RaxNode;
}
/**
 * 行列通用属性类型
 */
interface BaseRowColPropBases extends BaseProps {
    /**
     * 是否使用 flex 模式来渲染（注意：移动端部分场景会有兼容）
     */
    flex?: boolean;
    /**
     * 是否 宽度/高度 自适应
     */
    autoFit?: boolean;
    /**
     * 单元格列间距尺寸, 可为预设值 `small`， `medium`、`large`, `false` 或 css 长度单位。此项设置会覆盖 Page 上的 grid-gap 全局值
     */
    gap?: BaseGap;
    /**
     * 固定宽度
     */
    width?: number;
    /**
     * 固定高度
     */
    height?: number;
    /**
     * 自定义样式
     */
    style?: CSSProperties;
    /**
     * 孩子节点
     */
    children?: RaxNode;
}
/**
 * 行模式容器
 */
export interface RowProps extends BaseRowColPropBases {
    /**
     * 子元素垂直对齐方式
     */
    verAlign?: 'top' | 'middle' | 'bottom' | 'stretch' | 'baseline';
}
/**
 * 列模式容器
 */
export interface ColProps extends BaseRowColPropBases {
    /**
     * 子元素水平对齐方式
     */
    align?: 'left' | 'center' | 'right' | 'stretch';
}
/**
 * 字体
 */
export interface TextProps extends BaseProps {
    /**
     * 字体类型
     */
    type?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6' | 'body1' | 'body2' | 'caption' | 'overline';
    /**
     * 标记
     */
    mark?: boolean;
    /**
     * 删除线
     */
    delete?: boolean;
    /**
     * 下划线
     */
    underline?: boolean;
    /**
     * 加粗
     */
    strong?: boolean;
    /**
     * 代码样式
     */
    code?: boolean;
    /**
     * 缩进
     */
    indented?: boolean;
    /**
     * 颜色
     */
    color?: string;
    /**
     * 背景色
     */
    backgroundColor?: string;
    /**
     * 子节点
     */
    children?: RaxNode;
}
/**
 * 段落
 */
export interface ParagraphProps extends BaseProps {
    /**
     * 字体类型（Text type的快捷方式）
     */
    type?: TextProps['type'];
    /**
     * 水平对齐方式
     */
    align?: 'left' | 'right' | 'center' | 'space-between' | 'space-around' | 'space-evenly';
    /**
     * 垂直对齐方式
     */
    verAlign?: 'top' | 'middle' | 'bottom' | 'baseline';
    /**
     * 保留子元素间水平间距
     */
    spacing?: false | BaseSize;
    /**
     * 添加子元素上下外间距
     */
    verMargin?: boolean;
    /**
     * 段前间距尺寸（在父容器中的第一个段落无效）
     */
    beforeMargin?: number;
    /**
     * 段后间距尺寸 (在父容器中的最后一个段落无效)
     */
    afterMargin?: number;
    /**
     * 孩子节点
     */
    children?: RaxNode;
}
export {};
