import { FC } from 'rax';
import { TypeMark, RowProps } from '../types';
declare type IRowComponent = FC<RowProps> & TypeMark;
/**
 * 行拆分布局 （子元素如果不是 Row、Col 或 Cell, 则默认用 Cell 包裹）
 */
export declare const Row: IRowComponent;
export default Row;
