import { FC } from 'rax';
import { TypeMark, ColProps } from '../types';
declare type IColComponent = FC<ColProps> & TypeMark;
/**
 * 列拆分布局（子元素如果不是 Row、Col 或 Cell, 则默认用 Cell 包裹）
 */
export declare const Col: IColComponent;
export default Col;
