import { FC } from 'rax';
import { TypeMark, CellProps } from '../types';
declare type ICellComponent = FC<CellProps> & TypeMark;
/**
 * 单元格容器(默认垂直方向的 flex 布局容器)
 */
declare const Cell: ICellComponent;
export default Cell;
