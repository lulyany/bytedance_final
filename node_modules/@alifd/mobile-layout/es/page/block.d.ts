import { FC } from 'rax';
import { TypeMark, BlockProps } from '../types';
declare type IBlockComponent = FC<BlockProps> & TypeMark;
/**
 * 区块,默认 flex 布局
 * @param props
 * @constructor
 */
declare const Block: IBlockComponent;
export default Block;
