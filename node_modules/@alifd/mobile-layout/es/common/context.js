import { createContext } from 'rax';
import { DEFAULT_BREAK_POINTS } from './constant';
export var defaultContext = {
  prefix: 'mt-layout-',
  breakPoint: DEFAULT_BREAK_POINTS[0],
  maxNumberOfColumns: DEFAULT_BREAK_POINTS[DEFAULT_BREAK_POINTS.length - 1].numberOfColumns,
  noPadding: false,
  sectionGap: null,
  blockGap: null,
  gridGap: null
};
export default createContext(defaultContext);