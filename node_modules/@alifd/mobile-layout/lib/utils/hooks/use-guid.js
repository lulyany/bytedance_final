"use strict";

exports.__esModule = true;
exports.default = useGuid;
exports.guid = guid;

var _rax = require("rax");

var counter = 0;

function guid(prefix) {
  if (prefix === void 0) {
    prefix = '';
  }

  counter += 1;
  return "" + prefix + counter;
}

function useGuid(prefix) {
  if (prefix === void 0) {
    prefix = '';
  }

  var id = (0, _rax.useRef)(guid(prefix));
  return id.current;
}